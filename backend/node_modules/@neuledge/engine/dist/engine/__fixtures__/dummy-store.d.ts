import { Store } from '@neuledge/store';
export declare class DummyStore implements Store {
    listCollections(): Promise<never[]>;
    describeCollection(): Promise<never>;
    ensureCollection(): Promise<void>;
    dropCollection(): Promise<void>;
    find(): Promise<never[]>;
    insert(): Promise<{
        insertedIds: never[];
        affectedCount: number;
    }>;
    update(): Promise<{
        affectedCount: number;
    }>;
    delete(): Promise<{
        affectedCount: number;
    }>;
}
//# sourceMappingURL=dummy-store.d.ts.map