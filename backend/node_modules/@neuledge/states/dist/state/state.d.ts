import { Mutation } from '../mutation';
import { StateNode } from '@neuledge/states-parser';
import { StateField } from './field';
import { StateSortingIndex, StatePrimaryKey } from './state-index';
export interface State<N extends string = string> {
    type: 'State';
    node: StateNode;
    name: N;
    description?: string;
    deprecated?: boolean | string;
    fields: Record<string, StateField>;
    primaryKey: StatePrimaryKey;
    indexes: Record<string, StateSortingIndex>;
    mutations: Record<string, Mutation>;
    baseIndex: number;
}
export declare const parseState: (node: StateNode, fields: State['fields'], mutations: State['mutations'], baseIndex: State['baseIndex']) => State;
//# sourceMappingURL=state.d.ts.map